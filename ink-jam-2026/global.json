{
  "$comment": "Pins a .NET 10 SDK with the wasm-tools workload for the whole project, so the web host publishes from the project root too (global.json applies at or below its own directory).",
  "sdk": {
    "version": "10.0.100",
    "rollForward": "latestFeature"
  }
}
